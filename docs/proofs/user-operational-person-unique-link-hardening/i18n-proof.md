# i18n Proof: User ↔ OperationalPerson Unique Link

## Status: VERIFIED — NO CHANGES NEEDED

Translation keys are display strings only. They label UI fields like "Personnel Code", "Personnel Name", "Role", etc. The keys and values remain unchanged because:

1. The UI fields still show `code`, `name`, `role` at the same locations
2. No new fields were added to the maintenance personnel form
3. No labels changed

### Files verified
- `apps/web/src/lib/i18n/locales/en/maintenance.ts` — unchanged
- `apps/web/src/lib/i18n/locales/ar/maintenance.ts` — unchanged
- `apps/web/src/lib/i18n/locales/en/navigation.ts` — unchanged
- `apps/web/src/lib/i18n/locales/ar/navigation.ts` — unchanged
